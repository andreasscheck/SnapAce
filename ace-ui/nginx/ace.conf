location = /ace {
    return 301 /ace/;
}

location /ace/ {
    alias /usr/local/ace-ui/html/;
    index index.html;
    # This page changes across deploys during active development; avoid
    # browsers caching a stale script.js/style.css against a newer index.html.
    add_header Cache-Control "no-cache";
}
